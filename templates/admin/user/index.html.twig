{% extends 'admin/adminbase.html.twig' %}
{% block body %}

    <a class = "bg-green-700 border-none text-center no-underline inline-block text-base text-white" href="{{ path('admin_create') }}">Создать пользователя</a><br>
    <div class="container flex flex-col justify-left">
        <h2>
            {{ title }}
        </h2>
        <br>
        <ul>
            {% for user in users %}
                <li>{{user.id }} {{ user.email }}, {{user.roles | json_encode}}</li>
            {% endfor %}
        </ul> 
    </div>

{% endblock %}